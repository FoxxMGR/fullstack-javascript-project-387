{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "description": "Backend для Calendar Booking API — реализация контракта api-spec/main.tsp (хранилище в памяти)",
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=23.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.3"
  }
}